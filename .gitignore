# Dependencies
node_modules/

# Environment & secrets
.env
.env.local
.env.*.local

# Built Tailwind CSS (public/css/styles.css) is COMMITTED so Vercel can serve
# it without a build step. Run `npm run build:css` after editing styles and
# commit the result. (Re-add the ignore below if you switch to building on CI.)
# public/css/styles.css

# Compiled TypeScript output
dist/

# Vercel
.vercel

# Logs
logs/
*.log
npm-debug.log*

# Runtime data
pids/
*.pid

# OS / editor / tooling
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/

# Redundant packaged copy of the (already-committed) extracted logo package
Brand/veylrio-logo-package.zip

# Optional: keep brand source out of deploy if you prefer
# Brand/
